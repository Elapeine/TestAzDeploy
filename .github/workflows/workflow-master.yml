name: Master Call a reusable workflow

on:
  pull_request:
    branches:
      - main

jobs:

  call-workflow-passing-data:
    uses: Elapeine/TestAzDeploy/.github/workflows/workflow-B.yml@main
    with:
      username: mona
   # secrets:
   #   token: ${{ secrets.TOKEN }}
